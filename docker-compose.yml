name: auction-live

services:
  backend:
    build:
      context: ./Backend
      dockerfile: Dockerfile
    container_name: auction-backend
    ports:
      - "8080:8080"
    environment:
      APP_ENV: production
      APP_PORT: "8080"
      WS_ALLOWED_ORIGIN: "*"
    restart: unless-stopped
