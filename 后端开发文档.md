# 后端开发文档

## 1. 文档目标

这份文档面向这个竞赛项目的后端开发，重点是帮助初学者看清楚：

- 这个后端到底要做什么
- 推荐用什么技术栈
- 应该拆成哪些模块
- 一次出价从前端到数据库到底经历了什么
- 为什么这个项目会涉及高并发、实时通信、幂等、状态机这些概念
- 开发时应该先做什么，再做什么

你可以把这份文档理解成“后端总蓝图”。

---

## 2. 项目目标

这个项目的后端本质上是在做一个“实时竞拍引擎”。

它不只是普通的 CRUD 系统，而是要同时解决下面几类事情：

- 管理直播间
- 管理拍品
- 管理竞拍场次
- 接收用户出价
- 校验出价是否合法
- 在多人同时出价时保证结果正确
- 把最新价格和状态实时推送给前端
- 在竞拍结束后结算并生成结果

一句话理解：

后端最重要的职责，是在高并发实时场景下，保证“谁真的出价成功、当前价格是多少、竞拍什么时候结束、最终谁成交”这几件事永远是对的。

---

## 3. 推荐技术栈

## 3.1 主推荐方案

建议优先采用：

- 运行环境：`Node.js`
- 后端框架：`NestJS`
- 数据库：`MySQL`
- 缓存：`Redis`
- 实时通信：`Socket.IO`
- ORM：`Prisma`
- 接口文档：`Swagger`
- 参数校验：`class-validator`
- 定时任务：`@nestjs/schedule`
- 压测工具：`k6` 或 `autocannon`

---

## 3.2 为什么推荐这套

### `Node.js`

优点：

- 前后端语言统一，都是 JavaScript / TypeScript
- 资料多，AI 生成代码效果好
- 做实时通信比较方便

### `NestJS`

优点：

- 工程结构清晰
- 模块化强
- 很适合拆控制器、服务、网关、模块
- 对初学者来说比手写 Express 更容易维持结构

### `MySQL`

优点：

- 存业务主数据稳定
- 适合存用户、拍品、竞拍记录、订单等正式数据

### `Redis`

优点：

- 读写速度快
- 非常适合存当前价格、排行榜、当前领先者、结束时间等高频变化数据

### `Socket.IO`

优点：

- 支持 WebSocket 风格实时通信
- 有断线重连能力
- 房间机制很适合“按直播间广播”

---

## 3.3 适合初学者的结论

如果你是第一次做这种项目，不建议一开始上过于激进的方案。

这次最稳的路线是：

- `NestJS + MySQL + Redis + Socket.IO`

这样：

- 代码结构好管理
- AI 容易帮你搭骨架
- 实时通信和高并发基础能力都能覆盖

---

## 4. 后端总体架构

可以把后端拆成 5 层来理解：

### 第 1 层：接口层

负责接收请求。

包括：

- REST API
- WebSocket 事件

### 第 2 层：业务层

负责核心规则判断。

包括：

- 出价是否合法
- 当前竞拍是否进行中
- 是否触发延时
- 是否达到封顶价

### 第 3 层：实时状态层

负责高频变化的数据。

建议放在：

- `Redis`

比如：

- 当前价
- 当前领先者
- 排行榜
- 结束时间
- 房间在线人数

### 第 4 层：持久化层

负责正式落库。

建议放在：

- `MySQL`

比如：

- 用户
- 直播间
- 拍品
- 竞拍场次
- 出价记录
- 成交结果

### 第 5 层：推送层

负责把变化实时通知前端。

建议使用：

- `Socket.IO`

---

## 5. 功能模块拆分

建议至少拆成下面这些模块。

## 5.1 用户模块

作用：

- 模拟登录
- 获取用户信息
- 管理用户身份

初版可以简化为：

- 昵称登录
- 游客身份

---

## 5.2 直播间模块

作用：

- 获取直播间列表
- 获取直播间详情
- 管理直播间状态

你后续想做多直播间，这个模块必须预留好。

---

## 5.3 拍品模块

作用：

- 获取某个直播间下的拍品列表
- 标记当前拍品
- 查询拍品状态

拍品状态建议包括：

- `not_started`
- `bidding`
- `ended_unsold`
- `ended_sold`
- `closed`

---

## 5.4 竞拍场次模块

作用：

- 创建竞拍场次
- 启动竞拍
- 延时竞拍
- 结束竞拍
- 取消竞拍

这个模块是核心。

---

## 5.5 出价模块

作用：

- 接收出价请求
- 校验价格
- 更新当前价
- 更新领先者
- 写入出价记录
- 推送最新状态

这是整个系统最关键的模块。

---

## 5.6 排行和状态模块

作用：

- 维护当前拍品的领先者信息
- 维护用户自己的竞拍状态
- 维护当前房间下的拍品实时状态

注意：

示例图更像“当前领先状态”和“我的出价状态”，不一定需要传统排行榜页面，但后端仍然要能提供这些实时状态数据。

---

## 5.7 订单 / 结果模块

作用：

- 在竞拍结束后生成结果
- 记录最终成交价
- 记录成交用户
- 供前端展示“成交 / 流拍 / 已结束”

---

## 5.8 WebSocket 网关模块

作用：

- 处理连接和断开
- 按直播间分房间广播
- 推送价格变化
- 推送延时通知
- 推送结束通知

---

## 5.9 日志与监控模块

作用：

- 记录关键操作
- 记录异常
- 记录竞拍状态变化

初版哪怕没有完整监控平台，也建议先做操作日志。

---

## 6. 数据库设计

下面给一个适合初学者理解的表设计草图。

## 6.1 `users`

用户表。

字段建议：

- `id`
- `nickname`
- `avatar`
- `role`
- `created_at`
- `updated_at`

---

## 6.2 `live_rooms`

直播间表。

字段建议：

- `id`
- `title`
- `cover_image`
- `anchor_name`
- `status`
- `created_at`
- `updated_at`

---

## 6.3 `auction_items`

拍品表。

字段建议：

- `id`
- `room_id`
- `title`
- `cover_image`
- `description`
- `start_price`
- `increment_step`
- `max_price`
- `extension_seconds`
- `sort_order`
- `status`
- `created_at`
- `updated_at`

---

## 6.4 `auction_sessions`

竞拍场次表。

这个表比拍品表更偏“运行态”。

字段建议：

- `id`
- `room_id`
- `item_id`
- `start_time`
- `end_time`
- `current_price`
- `leader_user_id`
- `status`
- `version`
- `created_at`
- `updated_at`

说明：

- 一个拍品最好对应一个竞拍场次
- `version` 可以用于乐观锁思路

---

## 6.5 `bids`

出价记录表。

字段建议：

- `id`
- `session_id`
- `room_id`
- `item_id`
- `user_id`
- `bid_price`
- `request_id`
- `status`
- `created_at`

说明：

- `request_id` 很重要，用于幂等控制

---

## 6.6 `auction_results`

竞拍结果表。

字段建议：

- `id`
- `session_id`
- `item_id`
- `winner_user_id`
- `final_price`
- `result_status`
- `created_at`

结果状态建议：

- `sold`
- `unsold`
- `cancelled`

---

## 6.7 `operation_logs`

操作日志表。

字段建议：

- `id`
- `module`
- `action`
- `operator_id`
- `target_id`
- `content`
- `created_at`

---

## 7. Redis 设计

这个项目不要把所有东西都只放数据库里。

高频实时数据建议放 Redis。

## 7.1 为什么要 Redis

因为竞拍过程里变化特别频繁的数据包括：

- 当前价
- 当前领先者
- 当前拍品状态
- 倒计时结束时间
- 在线人数

这些数据如果每次都只靠 MySQL，会比较吃力，也不利于高并发。

---

## 7.2 建议缓存哪些数据

### 当前场次状态

例如：

- `auction:session:{sessionId}:state`

里面可以放：

- `currentPrice`
- `leaderUserId`
- `endTime`
- `status`
- `version`

### 当前拍品状态

例如：

- `auction:item:{itemId}:state`

### 出价幂等记录

例如：

- `auction:bid:req:{requestId}`

### 房间在线人数

例如：

- `auction:room:{roomId}:online`

---

## 7.3 初学者要理解的重点

MySQL 负责：

- 正式保存
- 历史记录
- 最终结果

Redis 负责：

- 高频读写
- 临时实时状态
- 快速判断

一句话：

`MySQL 管长期数据，Redis 管实时数据。`

---

## 8. REST API 和 WebSocket 怎么分工

这是很多初学者最容易搞混的地方。

## 8.1 REST API 负责什么

适合：

- 首次进入页面时拉完整数据
- 拉直播间列表
- 拉拍品列表
- 拉历史记录
- 提交出价请求

也就是：

`查完整数据` 和 `发正式操作请求`

---

## 8.2 WebSocket 负责什么

适合：

- 价格变化广播
- 当前拍品切换广播
- 延时通知
- 被超越通知
- 竞拍结束通知
- 在线人数变化

也就是：

`推实时变化`

---

## 8.3 一句话分工

- REST API：拉快照、发命令
- WebSocket：收变化、做同步

---

## 9. 一次完整出价流程

这是整个项目最关键的一段。

建议你反复理解。

## 9.1 用户视角

用户在前端点击“立即出价”。

然后用户会期待：

- 系统马上判断是否成功
- 如果成功，页面马上看到新价格
- 如果失败，收到明确提示

---

## 9.2 后端视角

一次出价建议按下面顺序处理：

1. 前端发起出价请求
2. 后端校验用户身份
3. 后端校验当前场次是否仍在 `bidding`
4. 校验价格是否满足加价幅度
5. 校验是否高于当前价格
6. 校验是否重复请求
7. 用 Redis 原子更新当前价格和领先者
8. 写入出价记录
9. 判断是否触发自动延时
10. 通过 WebSocket 广播最新价格和状态
11. 返回本次出价结果给前端

---

## 9.3 这里最重要的三个词

### 原子性

意思是：

这次出价的关键更新，要么一起成功，要么一起失败。

不能出现：

- 当前价改了，但记录没写
- 两个人同时都成功

### 幂等性

意思是：

同一个请求重复发过来，最多只处理一次。

### 一致性

意思是：

最终所有人看到的当前价、领先者、结果都必须一致。

---

## 10. 状态机设计

这个项目非常适合用状态机思路。

## 10.1 为什么要状态机

因为竞拍不是一直都能出价。

比如：

- 未开始时不能出价
- 已结束后不能出价
- 已取消后不能出价

所以必须让系统知道“当前处于什么状态”。

---

## 10.2 建议状态

### 场次状态

- `pending`：未开始
- `bidding`：竞拍中
- `ended`：已结束
- `cancelled`：已取消

### 拍品展示状态

- `not_started`
- `bidding`
- `ended_unsold`
- `ended_sold`
- `closed`

---

## 10.3 状态机的好处

- 防止非法操作
- 逻辑更清楚
- 更方便排查问题
- 更方便写代码和答辩

---

## 11. 高并发难点

这部分是竞赛里非常重要的技术深度。

## 11.1 难点 1：多人同时出价

问题：

- 100 个人可能同时点“立即出价”
- 如果处理不好，可能两个人都抢到同一个价格

解决思路：

- 热数据放 Redis
- 对单个场次做原子更新
- 使用 Lua 脚本或等价原子方案

---

## 11.2 难点 2：重复请求

问题：

- 用户手快点两次
- 网络超时重试
- 前端重复提交

解决思路：

- 每次出价带 `requestId`
- 后端记录这个 `requestId` 是否处理过
- 相同请求不重复执行

---

## 11.3 难点 3：结束和延时冲突

问题：

- 最后几秒有人出价
- 一个流程想结束，另一个流程想延时

解决思路：

- 结束和延时都必须由服务端统一判断
- 基于同一份场次状态做原子更新
- 避免前端自己决定是否结束

---

## 11.4 难点 4：多直播间隔离

问题：

- 你后面要做多个直播间
- 一个房间的消息不能串到另一个房间

解决思路：

- WebSocket 按 `roomId` 分房间
- Redis 状态按 `roomId / sessionId` 隔离
- 所有广播都只发给当前房间

---

## 11.5 难点 5：数据库压力

问题：

- 高频读写全落 MySQL，容易撑不住

解决思路：

- 热状态优先读 Redis
- MySQL 做最终落库
- 历史记录查库，当前态查缓存

---

## 12. 解决高并发的技巧

这里给你一个适合答辩的说法。

## 12.1 Redis 原子更新

用 Redis 处理：

- 当前价格
- 当前领先者
- 结束时间

好处：

- 快
- 可以原子处理

---

## 12.2 幂等控制

每次出价生成唯一 `requestId`。

好处：

- 防止重复请求
- 防止重复扣款式逻辑错误

---

## 12.3 房间隔离

按直播间隔离连接和广播。

好处：

- 结构清楚
- 更适合扩展多直播间

---

## 12.4 服务端统一时间

所有倒计时和结束判断都以服务端为准。

好处：

- 防止不同客户端时间不一致
- 防止前端显示和后端结果冲突

---

## 12.5 最终一致性

思路：

- Redis 先处理实时状态
- MySQL 再持久化
- 竞拍结束时做最终结算

好处：

- 实时性和稳定性兼顾

---

## 13. WebSocket 设计建议

## 13.1 建议推送的消息类型

- `room_joined`
- `auction_item_changed`
- `auction_price_updated`
- `auction_bid_success`
- `auction_overtaken`
- `auction_extended`
- `auction_ended`
- `room_online_count_updated`

---

## 13.2 推送内容建议

每类消息尽量包含关键字段。

例如价格更新消息建议包含：

- `roomId`
- `itemId`
- `sessionId`
- `currentPrice`
- `leaderUserId`
- `endTime`
- `status`
- `version`

这样前端更容易直接更新状态。

---

## 13.3 为什么要带 version

因为实时消息可能乱序到达。

带上 `version` 后：

- 新消息版本更大
- 旧消息版本更小
- 前端可以丢弃旧消息

---

## 14. 接口清单建议

## 14.1 REST API

- `GET /rooms`：获取直播间列表
- `GET /rooms/:roomId`：获取直播间详情
- `GET /rooms/:roomId/items`：获取拍品列表
- `GET /rooms/:roomId/current`：获取当前竞拍快照
- `GET /users/me/bids`：获取我的竞拍历史
- `POST /bids`：提交出价

---

## 14.2 WebSocket 事件

客户端发：

- `join_room`
- `leave_room`

服务端推：

- `auction_item_changed`
- `auction_price_updated`
- `auction_bid_success`
- `auction_overtaken`
- `auction_extended`
- `auction_ended`

---

## 15. 推荐目录结构

可以参考这样的 NestJS 目录拆法：

```text
src/
  app.module.ts
  common/
    guards/
    filters/
    interceptors/
    utils/
  modules/
    users/
    live-rooms/
    auction-items/
    auction-sessions/
    bids/
    results/
    websocket/
    logs/
  prisma/
  redis/
```

如果是初学者，记住一个原则：

- 按业务模块拆
- 不要把所有代码都堆在一个 service 里

---

## 16. 开发顺序建议

这个部分非常重要。

不要一上来就做所有功能。

## 16.1 第一阶段：先跑通最小闭环

先做这些：

- 用户伪登录
- 直播间列表
- 单直播间详情
- 单拍品竞拍
- 出价接口
- WebSocket 广播最新价格

这一步的目标是：

`一个房间里，两个人可以实时看到价格变化`

---

## 16.2 第二阶段：补竞拍规则

补这些规则：

- 起拍价
- 固定加价幅度
- 自动延时
- 封顶价
- 已结束不可出价

---

## 16.3 第三阶段：补完整状态

补这些：

- 流拍
- 成交
- 当前拍品切换
- 多拍品列表状态同步

---

## 16.4 第四阶段：补工程能力

补这些：

- 幂等
- Redis 原子逻辑
- 操作日志
- 压力测试
- 多直播间隔离

---

## 17. 测试建议

## 17.1 先做功能测试

重点验证：

- 能不能正常出价
- 加价规则对不对
- 已结束后还能不能出价
- 自动延时是否正确
- 成交和流拍是否正确

---

## 17.2 再做并发测试

重点验证：

- 多人同时出价时是否只有一个成功成为最高价
- 当前价是否连续正确
- 是否出现重复处理

---

## 17.3 最后做异常测试

重点验证：

- 断线重连后状态是否正确
- Redis 异常时是否有兜底
- 出价记录失败时是否能定位问题

---

## 18. 适合初学者的理解方式

如果你觉得这些概念很多，可以先抓住 4 句话。

### 第 1 句

MySQL 负责正式保存，Redis 负责实时状态。

### 第 2 句

REST API 负责拉完整数据和提交正式请求，WebSocket 负责实时广播变化。

### 第 3 句

出价逻辑必须保证原子性、幂等性和一致性。

### 第 4 句

整个后端的核心不是页面，而是“谁出价成功”和“最终结果是否绝对正确”。

---

## 19. 答辩时可以怎么讲

如果后面答辩讲后端思路，你可以用这种简单表达：

- 我们用 `NestJS` 搭建后端骨架，因为模块化更清楚
- 我们用 `MySQL` 存正式业务数据，用 `Redis` 存实时竞拍状态
- 我们用 `Socket.IO` 按直播间广播实时消息
- 我们用幂等 `requestId` 防止重复出价
- 我们用状态机控制竞拍只能在合法状态下流转
- 我们把高频实时状态和持久化数据分开处理，保证性能和一致性

---

## 20. 最终建议

对于这个竞赛，后端最好的策略不是一开始追求“最复杂”，而是：

- 先做最小可用闭环
- 先保证规则正确
- 再做高并发优化
- 最后补工程细节和演示效果

一句话总结：

这个项目的后端本质是一个“带实时推送能力的竞拍状态机系统”，而不是普通信息管理系统。

