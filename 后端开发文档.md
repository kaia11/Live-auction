# 后端开发文档

## 1. 文档目标

这份文档用于统一你们对“直播竞拍后端”这件事的业务理解和工程设计，避免前后端、演示原型、答辩口径各说各话。

它重点回答 6 个问题：

- 这个项目真实要做的业务闭环是什么
- 直播间、拍品、竞拍场次之间到底是什么关系
- 为什么界面上会同时看到多个拍品，但后端仍然应该按“单场次”驱动
- 后端应该拆哪些模块
- 高并发实时出价应该怎么保证正确性
- 第一版该先做什么，后面再补什么

---

## 2. 先统一业务理解

先说结论：

- 一个直播间里会展示多个拍品
- 这些拍品通常按队列依次推进，不是所有拍品同时竞价
- 任一时刻，一个直播间通常只有一个“当前激活竞拍场次”
- 其他拍品处于 `待开始 / 即将开始 / 已结束 / 已成交 / 已取消` 等状态

这也是为什么示意图里你会同时看到：

- 底部拍品列表
- 当前竞拍面板
- 当前价
- 我的出价
- 排名/领先状态

所以这里要把两个概念分开：

### 2.1 拍品列表

它是直播间里的“商品队列”或“拍品池”。

用户能看到多个拍品卡片，是为了：

- 知道后面还有哪些拍品
- 知道哪些拍品已经结束
- 快速查看当前拍品规则和状态

### 2.2 当前竞拍场次

它是后端真正实时推进的核心对象。

一个场次只绑定一个拍品，并且承载：

- 当前价
- 当前领先者
- 当前排行榜
- 倒计时结束时间
- 自动延时
- 封顶成交
- 结束/取消

所以你原来文档里的“当前拍品切换广播”这个说法不算完全错，但不够准确。

更准确的表达应该是：

- `当前激活竞拍场次切换广播`
- 或 `拍品队列推进广播`
- 或 `当前竞拍标的变更广播`

因为前端看到的不只是“换了一张商品卡”，而是：

- 当前场次结束
- 下一个拍品进入激活状态
- 列表里多个拍品状态一起变化
- 当前价、倒计时、排行榜、我的状态全部切到新场次

---

## 3. 项目真正要做的事

这个后端本质上不是普通电商 CRUD，而是一个“直播场景下的实时竞拍状态机系统”。

核心目标是同时做好 4 件事：

- 维护直播间和拍品队列
- 维护当前激活竞拍场次
- 在多人同时出价时保证最高价和领先者永远正确
- 把场次状态、排行榜、倒计时、结果实时同步给前端

一句话总结：

后端最核心的职责，是保证“谁当前领先、当前价格是多少、什么时候结束、最终谁成交”这几件事在高并发下仍然绝对正确。

---

## 4. 结合题目 PDF 后，建议这样理解产品形态

从宣讲版文字和示意图可以提炼出几个关键事实：

- 用户端是“直播间 + 竞拍插件”的形态，不是独立商城下单页
- 页面里会展示拍品列表，但通常只有一个激活竞拍对象
- 用户端需要实时看到自己的位置或领先状态，因此后端要支持排行榜/名次能力
- 主播/商家端需要配置规则、查看状态、取消异常竞拍，因此后端不能只做用户端接口
- 结果闭环不只到“出价成功”，还要到“成交结果 / 订单记录 / 历史竞拍记录”

所以后端设计不能只围绕“单个拍品出价接口”，而要围绕下面这个闭环：

`直播间 -> 拍品队列 -> 当前激活场次 -> 实时出价 -> 排名同步 -> 场次结束 -> 结果落库 -> 下一个场次激活`

---

## 5. 推荐技术栈

### 5.1 主推荐方案

建议优先采用：

- 运行环境：`Go`
- HTTP 框架：标准库 `net/http` 或 `Gin`
- 数据库：`MySQL`
- 缓存：`Redis`
- 实时通信：`WebSocket`
- 数据访问：`GORM` 或 `sqlx`
- 接口文档：`Swagger`
- 定时任务：Go 定时器 / 独立调度协程
- 压测工具：`k6` 或 `autocannon`

### 5.2 为什么这套适合当前项目

- `Go`：并发模型天然适合高频实时出价、房间广播、状态更新
- `MySQL`：适合存正式业务数据和历史记录
- `Redis`：适合扛“当前价、排行榜、倒计时”等高频实时状态
- `WebSocket`：适合做直播间房间隔离、断线重连、实时广播

### 5.3 对初学者的落地建议

第一版不要上过度复杂的微服务。

先做单体应用即可：

- `Go + MySQL + Redis + WebSocket`

先把业务正确性、实时性、演示效果做稳，再考虑横向扩展。

---

## 6. 核心业务对象

建议统一成下面 6 个核心对象。

### 6.1 用户 `user`

竞拍参与者、主播、后台运营都属于用户，只是角色不同。

### 6.2 直播间 `live_room`

一个直播间承载一组拍品和多场竞拍。

### 6.3 拍品 `auction_item`

拍品是静态业务对象，描述“卖什么”以及默认规则。

例如：

- 标题
- 图片
- 描述
- 起拍价
- 加价幅度
- 封顶价
- 延时规则

当前版本的规则约定为：

- 支持 `0 元起拍`
- 封顶价由商家按拍品单独设置
- 如果封顶价不设置，则视为 `无封顶`
- 加价幅度按拍品单独设置
- 每个拍品只允许拍卖 `1 次`

### 6.4 拍品队列 `room_item_queue`

它描述某个直播间里，这些拍品的展示和出场顺序。

这层很重要，因为你们不是孤立拍一个商品，而是：

- 一个房间有多个拍品
- 拍品有排序
- 前一个结束后，下一个进入 `即将开始 / 待开始` 状态
- 是否真正开始下一场，由主播或商家手动控制

另外要明确：

- 一个直播间同一时刻只能有 `1` 个激活场次
- 拍品默认按队列顺序开拍
- 主播可以手动切换到下一个拍品
- 场次结束后，系统只推进到下一件 `upcoming/pending`
- 下一场的真正开始时间由主播手动触发
- 已拍卖过的拍品不重复开拍

### 6.5 竞拍场次 `auction_session`

这是最核心的运行态对象。

一个场次绑定一个拍品，承载实时状态：

- 是否开始
- 当前价
- 当前领先者
- 当前参与人数
- 倒计时
- 是否延时
- 是否结束

### 6.6 出价记录 `bid`

每次用户出价都会形成出价记录，用于：

- 审计
- 历史记录
- 排查争议
- 生成排行榜和结果

---

## 7. 推荐的总体架构

可以把后端拆成 6 层理解：

### 第 1 层：接入层

- REST API
- WebSocket Gateway

### 第 2 层：鉴权与校验层

- 登录态校验
- 角色校验
- DTO 参数校验

### 第 3 层：核心业务层

- 房间管理
- 拍品队列管理
- 场次状态机
- 出价引擎
- 排名计算
- 结果结算

### 第 4 层：实时状态层

主要放在 `Redis`：

- 当前场次状态
- 当前价
- 当前领先者
- 排行榜
- 倒计时结束时间
- 在线人数

### 第 5 层：持久化层

主要放在 `MySQL`：

- 用户
- 直播间
- 拍品
- 队列顺序
- 场次记录
- 出价记录
- 成交结果
- 操作日志

### 第 6 层：消息推送层

通过 `WebSocket` 向对应直播间广播：

- 当前价变化
- 排行变化
- 场次延时
- 场次结束
- 场次切换
- 列表状态更新

---

## 8. 功能模块拆分

建议至少拆成下面这些模块。

## 8.1 用户与认证模块

作用：

- 模拟登录
- 获取当前用户信息
- 管理用户角色

角色建议至少包括：

- `viewer`
- `anchor`
- `admin`

第一版可以简化为：

- 昵称登录
- 游客参与

## 8.2 直播间模块

作用：

- 获取直播间列表
- 获取直播间详情
- 管理直播间开播/关闭状态
- 记录当前激活场次 ID

## 8.3 拍品模块

作用：

- 创建拍品
- 修改未开始拍品
- 获取房间内拍品列表
- 管理拍品展示状态
- 提供拍品详情数据

要注意：

- 拍品不等于当前场次
- 拍品是静态业务配置
- 场次是实时运行实例

## 8.4 拍品队列 / 排期模块

这是原文档里缺失较明显的一层。

作用：

- 维护某直播间的拍品顺序
- 标记当前激活拍品
- 场次结束后推进到下一个待开始拍品
- 支持调整未开始拍品顺序
- 支持主播手动切换到下一个拍品

如果没有这一层，后面“当前拍品切换”会变得很模糊。

## 8.5 竞拍场次模块

作用：

- 创建场次
- 启动场次
- 自动延时
- 封顶成交
- 正常结束
- 异常取消
- 将下一件拍品置为待开始
- 由商家手动开始下一场

这是全系统的状态机核心。

## 8.6 出价模块

作用：

- 接收出价请求
- 校验是否满足规则
- 原子更新当前价和领先者
- 写入出价记录
- 更新排行榜
- 推送实时变化

当前版本还要预留两类扩展能力：

- 代理出价 / 智能跟随
- 客户端振动提醒触发信号

这是并发压力最大的模块。

## 8.7 排行与个人状态模块

原文档里把这里写得偏轻了，但从题目要求看，这一层应该明确存在。

作用：

- 维护当前场次排行榜
- 计算用户个人名次
- 维护“我是否领先”
- 维护“我是否被超越”
- 提供前 N 名和我的名次查询

当前版本的榜单范围建议固定为：

- 前 `3` 名排行榜
- 我的当前名次
- 我的最高有效出价

注意：

- 不一定要做很复杂的完整排行榜页面
- 但后端必须能支撑“动态排名”与“我的位置”

## 8.8 结果与订单模块

作用：

- 竞拍结束后生成结果
- 标记成交 / 流拍 / 取消
- 生成模拟订单
- 给前端提供结果查询

第一版订单管理建议控制范围为：

- 生成模拟订单
- 查看成交金额
- 查看买家信息
- 查看订单状态

不要求实现真实物流履约系统，物流详情可作为后续扩展能力预留字段。

## 8.9 主播/后台管理模块

这是题目 PDF 明确要求的，不应缺失。

作用：

- 发布拍品
- 配置规则
- 查看全部场次状态
- 修改未开始场次
- 取消异常竞拍
- 查看成交订单
- 手动切换下一件拍品
- 查看数据统计面板

## 8.10 WebSocket 网关模块

作用：

- 连接管理
- 房间订阅与退订
- 在线人数维护
- 向直播间广播场次变化
- 向用户定向推送“你被超越了”等消息

## 8.11 日志与监控模块

作用：

- 记录关键业务操作
- 记录出价异常
- 记录状态流转
- 记录 Redis / MySQL 异常

初版没必要上完整 observability 平台，但日志一定要做。

---

## 9. 正确的业务状态模型

这里建议不要只定义“拍品状态”，而是拆成 3 套状态。

## 9.1 直播间状态

建议：

- `draft`
- `live`
- `paused`
- `closed`

## 9.2 拍品队列状态

表示该拍品在直播间队列里的展示位置。

建议：

- `queued`：已入队，未轮到
- `upcoming`：即将开始
- `active`：当前对应激活场次
- `finished`：已结束
- `cancelled`：被取消

主播端商品管理展示时，可映射为更业务化的文案：

- `待上架`
- `未开始`
- `即将开始`
- `竞拍中`
- `已成交`
- `已流拍`
- `已取消`
- `已结束`

## 9.3 竞拍场次状态

表示当前场次的真实业务状态。

建议：

- `pending`：已创建未开始
- `bidding`：竞拍中
- `ended_sold`：已成交结束
- `ended_unsold`：流拍结束
- `cancelled`：异常取消

这样拆的好处是：

- 拍品列表状态更清楚
- 当前激活场次更清楚
- 前端展示和后端状态机不会混在一起

---

## 10. 数据库设计建议

下面给一个更贴近题目场景的表设计草图。

## 10.1 `users`

字段建议：

- `id`
- `nickname`
- `avatar`
- `role`
- `created_at`
- `updated_at`

## 10.2 `live_rooms`

字段建议：

- `id`
- `title`
- `cover_image`
- `anchor_user_id`
- `status`
- `current_session_id`
- `created_at`
- `updated_at`

说明：

- `current_session_id` 方便快速定位当前激活场次

## 10.3 `auction_items`

字段建议：

- `id`
- `title`
- `cover_image`
- `description`
- `start_price`
- `increment_step`
- `ceiling_price`
- `duration_seconds`
- `extension_seconds`
- `extension_trigger_seconds`
- `created_by`
- `created_at`
- `updated_at`

说明：

- 规则字段可以放在拍品表
- 如果担心后续修改规则影响历史，也可以在场次表冗余一份快照
- `start_price` 允许为 `0`
- `ceiling_price` 为空表示无封顶
- `increment_step` 按拍品单独配置

## 10.4 `room_item_queue`

这是原文档建议补充的新表。

字段建议：

- `id`
- `room_id`
- `item_id`
- `sort_order`
- `queue_status`
- `scheduled_start_time`
- `actual_start_time`
- `actual_end_time`
- `created_at`
- `updated_at`

作用：

- 维护某直播间内拍品顺序
- 维护哪些已经结束、哪些还没开始

## 10.5 `auction_sessions`

字段建议：

- `id`
- `room_id`
- `item_id`
- `queue_id`
- `start_time`
- `end_time`
- `final_end_time`
- `start_price_snapshot`
- `increment_step_snapshot`
- `ceiling_price_snapshot`
- `extension_seconds_snapshot`
- `current_price`
- `leader_user_id`
- `participant_count`
- `status`
- `version`
- `created_at`
- `updated_at`

说明：

- 建议保留规则快照，避免拍品规则被修改后影响历史场次解释
- `version` 用于乐观锁 / 消息版本控制

## 10.6 `bids`

字段建议：

- `id`
- `session_id`
- `room_id`
- `item_id`
- `user_id`
- `bid_price`
- `rank_after_bid`
- `request_id`
- `status`
- `created_at`

说明：

- `request_id` 用于幂等
- `rank_after_bid` 可选，用于追溯当时名次

## 10.7 `auction_results`

字段建议：

- `id`
- `session_id`
- `room_id`
- `item_id`
- `winner_user_id`
- `final_price`
- `participant_count`
- `result_status`
- `ended_at`
- `created_at`

结果状态建议：

- `sold`
- `unsold`
- `cancelled`

## 10.8 `orders`

如果你们要把“成交后生成订单”做得更完整，建议补这一张。

字段建议：

- `id`
- `result_id`
- `room_id`
- `item_id`
- `buyer_user_id`
- `amount`
- `order_status`
- `created_at`
- `updated_at`

## 10.9 `operation_logs`

字段建议：

- `id`
- `module`
- `action`
- `operator_id`
- `target_type`
- `target_id`
- `content`
- `created_at`

---

## 11. Redis 设计建议

高频实时数据不要全走 MySQL。

## 11.1 建议缓存哪些内容

### 当前场次状态

例如：

- `auction:session:{sessionId}:state`

字段建议：

- `currentPrice`
- `leaderUserId`
- `endTime`
- `status`
- `version`
- `participantCount`

### 当前场次排行榜

例如：

- `auction:session:{sessionId}:ranking`

建议用有序集合存：

- `score = 用户最高有效出价`
- `member = userId`

### 当前用户在场次中的状态

例如：

- `auction:session:{sessionId}:user:{userId}`

可缓存：

- `myHighestBid`
- `currentRank`
- `isLeading`

### 出价幂等记录

例如：

- `auction:bid:req:{requestId}`

### 房间在线人数

例如：

- `auction:room:{roomId}:online`

### 当前房间激活场次

例如：

- `auction:room:{roomId}:current_session`

## 11.2 MySQL 和 Redis 的分工

MySQL 负责：

- 正式保存
- 历史查询
- 结果留档

Redis 负责：

- 高频状态
- 实时排行
- 快速原子判断

一句话：

`MySQL 管历史和结果，Redis 管当前赛况。`

---

## 12. REST API 和 WebSocket 的正确分工

## 12.1 REST API 负责什么

适合：

- 首次进入页面拉完整快照
- 获取直播间列表
- 获取拍品列表
- 获取当前场次详情
- 获取排行榜快照
- 获取我的竞拍历史
- 提交正式出价请求
- 后台创建/修改/取消场次

也就是：

`查快照 + 发正式命令`

## 12.2 WebSocket 负责什么

适合：

- 价格变化广播
- 排名变化广播
- 当前激活场次切换广播
- 列表状态更新广播
- 自动延时通知
- 被超越通知
- 成交/流拍/取消通知
- 在线人数变化
- 客户端振动提醒触发信号

也就是：

`推实时变化`

## 12.3 原文档里需要修正的一个点

不要把 WebSocket 的职责写成只推“当前拍品切换”。

更完整地说，WebSocket 应该推送的是“当前场次实时状态流”：

- 场次切换
- 当前价变化
- 名次变化
- 延时
- 结束
- 关联拍品列表状态变化

---

## 13. 一次完整出价流程

这是整个系统最关键的链路。

建议按下面顺序处理：

1. 前端提交 `POST /bids`
2. 后端校验用户身份和直播间/场次归属
3. 校验场次是否仍处于 `bidding`
4. 校验出价是否满足起拍价和加价幅度
5. 校验是否高于当前有效价格
6. 校验 `requestId` 是否已处理过
7. 通过 Redis Lua / 原子逻辑更新：
   - 当前价
   - 当前领先者
   - 当前排行榜
   - 场次版本号
8. 写入 `bids`
9. 必要时异步或事务内回写 `auction_sessions.current_price`
10. 判断是否触发自动延时
11. 判断是否达到封顶价并直接成交
12. 通过 WebSocket 广播价格、排名、个人状态变化
13. 返回本次出价结果

这条链路里最重要的是 4 个词：

- 原子性
- 幂等性
- 顺序性
- 一致性

当前版本的规则定义要明确成下面这样：

- 起拍价允许为 `0`
- 新出价必须满足 `bidPrice >= currentPrice + incrementStep`
- 新出价相对当前价的增量，必须是 `incrementStep` 的整数倍
- 加价幅度由该拍品单独决定
- 如果封顶价存在且 `bidPrice >= ceilingPrice`，则直接按封顶价成交
- 如果封顶价不存在，则本场无封顶
- 如果在结束前最后 `30s` 内有人出价，则在当前结束时间基础上 `+30s`
- 延时次数不设上限

---

## 14. 当前场次切换的正确流程

这个部分是为了替换原文档里模糊的“当前拍品切换广播”。

当一个场次结束时，后端应该做的是：

1. 将当前 `auction_session` 标记为结束或取消
2. 生成 `auction_result`
3. 更新 `room_item_queue.queue_status`
4. 查找下一个待开始拍品
5. 将下一个拍品标记为 `upcoming`
6. 将下一个 `auction_session` 保持为 `pending`
7. 等待主播或商家手动开始下一场
8. 更新 `live_rooms.current_session_id`
9. 同步 Redis 当前场次缓存
10. 向房间广播：
   - 上一场次已结束
   - 下一拍品已进入待开始
   - 拍品列表状态已更新

如果主播手动切换下一件拍品，也应复用同一套队列推进流程，只是触发原因不同：

- 正常结束切换
- 异常取消切换
- 主播手动切换

所以前端看到的不是“自动开拍下一件”，而是“上一件结束后先切到待开始，再由主播手动开始”。

建议事件名使用：

- `auction_session_ended`
- `auction_session_activated`
- `auction_session_upcoming`
- `room_item_queue_updated`

比单独一个 `auction_item_changed` 更准确。

如果想兼容前端已有叫法，可以保留：

- `auction_item_changed`

但语义上要明确：它表示“当前激活场次对应拍品已切换”，而不只是列表里点选了另一个商品。

---

## 15. 状态机设计

这个项目非常适合状态机。

## 15.1 为什么必须做状态机

因为竞拍里很多动作都只允许在特定状态发生。

例如：

- 未开始不能出价
- 已结束不能出价
- 已取消不能出价
- 已封顶成交后不能继续出价

## 15.2 关键状态流转

### 场次流转

- `pending -> bidding`
- `bidding -> ended_sold`
- `bidding -> ended_unsold`
- `bidding -> cancelled`

### 队列流转

- `queued -> upcoming`
- `upcoming -> active`
- `active -> finished`
- `active -> cancelled`

说明：

- `queued -> upcoming` 可以由系统在上一场结束后自动推进
- `upcoming -> active` 必须由主播或商家手动触发开始

## 15.3 状态机的收益

- 防止非法出价
- 方便做后台取消
- 方便写测试
- 方便答辩解释系统正确性

---

## 16. 高并发难点

## 16.1 多人同时出价

风险：

- 多人同时抢同一价格
- 当前价被覆盖
- 排名错乱

解决思路：

- 单场次热数据放 Redis
- 单场次使用 Lua 脚本原子更新
- 只允许一个请求成为该价格下的有效领先者

## 16.2 重复请求

风险：

- 用户连续点击
- 网络重试
- 同一请求被处理多次

解决思路：

- 每次出价带 `requestId`
- Redis/MySQL 记录该请求是否已处理
- 相同请求直接返回第一次结果

## 16.3 结束与延时冲突

风险：

- 最后几秒有人出价
- 一个流程准备结束，另一个流程准备延时

解决思路：

- 统一由服务端按场次状态判断
- 延时和结束基于同一份原子状态
- 前端只展示，不裁决

额外规则：

- 达到封顶价时直接成交，不再进入延时流程

## 16.4 排名同步错乱

这是原文档里缺失的一个重要并发点。

风险：

- 价格更新到了，排名没更新
- 用户收到被超越通知，但页面还显示自己领先

解决思路：

- 价格、领先者、版本号、排名尽量在同一次原子更新里产出
- 广播时带 `version`
- 前端按版本丢弃旧消息

## 16.5 多直播间隔离

风险：

- A 房间消息串到 B 房间
- 在线人数、当前价相互污染

解决思路：

- WebSocket 按 `roomId` 分组
- Redis key 按 `roomId / sessionId` 隔离
- 所有广播明确携带 `roomId`

---

## 17. WebSocket 事件建议

## 17.1 客户端发起

- `join_room`
- `leave_room`
- `subscribe_session`
- `unsubscribe_session`

## 17.2 服务端广播

- `room_joined`
- `room_online_count_updated`
- `auction_price_updated`
- `auction_ranking_updated`
- `auction_bid_success`
- `auction_overtaken`
- `auction_extended`
- `auction_session_ended`
- `auction_session_activated`
- `auction_session_upcoming`
- `room_item_queue_updated`
- `auction_detail_opened`
- `device_vibrate_signal`

## 17.3 消息体建议包含的关键字段

至少包括：

- `roomId`
- `sessionId`
- `itemId`
- `version`
- `serverTime`

价格变化类消息还建议包含：

- `currentPrice`
- `leaderUserId`
- `participantCount`
- `endTime`
- `topRanks`

这样前端才能少拼装、多直出。

---

## 18. 接口清单建议

这里建议分用户端接口和主播/后台接口。

## 18.1 用户端 REST API

- `GET /rooms`：获取直播间列表
- `GET /rooms/:roomId`：获取直播间详情和当前激活场次摘要
- `GET /rooms/:roomId/items`：获取拍品列表/队列状态
- `GET /rooms/:roomId/items/:itemId`：获取拍品详情
- `GET /rooms/:roomId/current-session`：获取当前竞拍快照
- `GET /sessions/:sessionId/ranking`：获取排行榜快照
- `GET /users/me/bids`：获取我的竞拍历史
- `POST /bids`：提交出价

补充说明：

- 用户端拍品列表里每个拍品应支持 `详情` 和 `立即出价`
- 点击拍品卡片本身进入拍品详情页
- 点击 `立即出价` 直接拉起出价面板

## 18.2 主播/后台 REST API

- `POST /admin/rooms/:roomId/items`：创建拍品
- `PATCH /admin/items/:itemId`：修改未开始拍品
- `POST /admin/rooms/:roomId/queue/reorder`：调整队列顺序
- `POST /admin/rooms/:roomId/queue/next`：手动切换到下一件拍品
- `POST /admin/sessions/:sessionId/start`：开始竞拍
- `POST /admin/sessions/:sessionId/cancel`：取消异常竞拍
- `GET /admin/rooms/:roomId/sessions`：查看全部场次状态
- `GET /admin/orders`：查看订单结果
- `GET /admin/stats/overview`：查看后台静态统计页
- `GET /admin/stats/timeline`：查看拍品出价时间线

---

## 19. 推荐目录结构

可以参考这样的 Go 目录拆法：

```text
Backend/
  cmd/
    server/
      main.go
  internal/
    app/
    config/
    http/
      handler/
    service/
    repository/
    model/
    ws/
  prisma/
```

这里比原文档多出来的重点模块是：

- `item-queue`
- `ranking`
- `orders`
- `admin`

因为它们都和题目要求直接相关。

---

## 20. 开发顺序建议

不要一上来做全量系统。

## 20.1 第一阶段：跑通最小闭环

先做：

- 伪登录
- 直播间列表
- 单直播间详情
- 拍品列表
- 单个当前激活场次
- 出价接口
- WebSocket 价格广播

目标：

`一个房间里，多人对当前场次实时出价，并看到同一份当前价`

## 20.2 第二阶段：补场次规则

补：

- 固定加价幅度
- 自动延时
- 封顶成交
- 已结束不可出价
- 异常取消

## 20.3 第三阶段：补队列推进和排名

补：

- 场次结束后切到下一拍品
- 拍品列表状态同步
- 实时排行榜
- 我的名次 / 我的领先状态

## 20.4 第四阶段：补后台和工程能力

补：

- 主播/后台管理接口
- 幂等
- Redis Lua 原子逻辑
- 操作日志
- 压测
- 多直播间隔离

---

## 21. 测试建议

## 21.1 功能测试

重点验证：

- 起拍价和加价幅度是否正确
- 是否只能按加价幅度的整数倍出价
- 封顶价是否自动成交
- 已结束后能否拦截出价
- 取消场次后状态是否正确
- 队列是否能正确推进到下一场
- 主播手动切换下一件拍品是否正确
- 点击详情和立即出价是否走向不同交互

## 21.2 实时同步测试

重点验证：

- 多端看到的当前价是否一致
- 排行榜是否同步
- 被超越提醒是否正确
- 延时通知是否正确

## 21.3 并发测试

重点验证：

- 多人同时出价时是否只有一个领先者
- 价格是否连续递增
- 相同 `requestId` 是否不会重复处理

## 21.4 异常测试

重点验证：

- 断线重连后是否能重新同步快照
- Redis 异常时是否有错误日志
- MySQL 落库失败时是否能定位问题

---

## 22. 适合答辩的表达

如果后面答辩讲后端设计，可以这样概括：

- 我们把直播间里的多个拍品设计成一个拍品队列，但任一时刻只激活一个竞拍场次
- 后端核心不是商品展示，而是当前场次状态机和高并发出价引擎
- MySQL 存正式业务数据和历史结果，Redis 存当前场次实时状态和排行榜
- WebSocket 不只是推价格，还负责推场次切换、排名变化、延时和结果通知
- 每个拍品都可以单独配置起拍价、加价幅度和封顶价，未设置封顶价则视为无封顶
- 系统支持最后 30 秒无限延时，直到无人继续出价或直接达到封顶价成交
- 我们通过 `requestId`、Redis 原子更新、版本号广播来保证幂等性和实时一致性

此外还可以说明我们预留了两个扩展方向：

- 代理出价 / 智能跟随
- 客户端振动提醒

---

## 23. 最终结论

这份文档修正后的核心业务认识是：

- 直播间里确实会同时展示多个拍品
- 但后端竞拍引擎通常不是“多件同时拍”，而是“拍品队列 + 单当前激活场次”
- 因此前后端都应围绕“队列推进 + 当前场次状态机 + 实时排名同步”来设计

一句话总结：

这个项目的后端本质上是一个“直播间多拍品队列驱动的单场次实时竞拍系统”。
