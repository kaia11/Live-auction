# 提纲

## 1. 题目理解

本次竞赛题目是一个“直播竞拍全栈系统”。

核心目标不是单纯做几个页面，而是完整实现以下业务闭环：

- 商品上架
- 规则配置
- 实时出价
- 动态排名
- 竞拍成交

系统需要同时覆盖两类角色：

- 商家/主播端：负责发布拍品、配置规则、管理竞拍、查看订单
- 用户端：负责进入直播间、查看竞拍信息、实时出价、查看结果

题目的重点在于：

- 实时通信能力
- 高并发下的数据一致性
- 复杂竞拍规则的正确实现
- 系统稳定性和可扩展性

对于开发小白，建议优先走“容易学习、资料多、AI 辅助效果好”的技术路线。

推荐整体技术栈：

- 前端：`React + TypeScript + Vite`
- 后端：`Node.js + NestJS`
- 实时通信：`Socket.IO`
- 数据库：`MySQL`
- 缓存与高并发支撑：`Redis`

---

## 2. 总体开发策略

建议不要一开始就追求“大而全”，而是采用“小步快跑”的方式。

推荐顺序：

1. 先做最小可用版本
2. 再补完整竞拍规则
3. 再补用户体验和动画效果
4. 最后补高并发优化、压测、监控和答辩材料

最小可用版本建议只做：

- 单直播间
- 单个竞拍商品
- 用户实时出价
- 实时价格同步
- 实时排行榜
- 竞拍结束并生成成交结果

这样做的好处是：

- 更容易边学边做
- 更容易让 AI 帮你生成代码
- 更容易定位 Bug
- 更适合竞赛时间有限的场景

---

## 3. 前端提纲

### 3.1 前端目标

前端建议分为两个部分：

- 用户端：移动端 H5 页面
- 商家/主播端：PC 管理后台

如果时间紧，优先级建议如下：

1. 先完成用户端 H5
2. 再补商家/主播后台

因为用户端更能体现竞拍的实时性，也是演示时最容易出效果的部分。

### 3.2 前端推荐技术栈

- `React`
- `TypeScript`
- `Vite`
- `React Router`
- `Zustand`
- `Axios`
- `TanStack Query`
- `Socket.IO Client`
- 用户端 UI：`antd-mobile`
- 后台端 UI：`Ant Design`
- 动画：`Framer Motion` 或 CSS 动画

技术栈说明：

- `React + TypeScript`：主流、资料多、适合组件化开发
- `Vite`：启动快，开发体验好
- `Zustand`：比 Redux 更轻，适合小白快速上手
- `TanStack Query`：适合处理接口请求、缓存、刷新
- `Socket.IO Client`：适合做实时消息同步，重连能力更强

### 3.3 前端页面拆分

#### 用户端页面

- 登录/进入直播间页
- 直播间主页
- 竞拍结果页
- 历史竞拍记录页
- 模拟支付页

#### 直播间主页建议包含的模块

- 视频区
- 当前竞拍商品卡片
- 商品详情
- 当前最高价
- 倒计时
- 出价按钮
- 出价记录列表
- 实时排行榜
- 我的竞拍状态
- 系统通知区域

#### 商家/主播后台页面

- 登录页
- 商品发布页
- 竞拍规则配置页
- 竞拍管理页
- 商品管理页
- 订单管理页

### 3.4 前端核心功能

- 展示直播间信息
- 展示竞拍商品信息
- 展示当前价格和倒计时
- 用户点击出价
- 实时接收最新价格和排名
- 实时接收“被超越”“领先”“延时”“结束”等通知
- 展示成交结果
- 支持断线重连后的状态恢复

### 3.5 前端难题

#### 难题 1：倒计时不准

常见问题：

- 前端自己本地计时会产生误差
- 不同用户手机时间不一致
- 网络延迟会导致页面显示不同步

解决技巧：

- 以前端展示“服务端下发的结束时间”为准
- 前端只负责渲染，不负责决定竞拍是否结束
- 每次收到 WebSocket 消息时同步一次服务端时间
- 页面重连后重新拉取当前结束时间

#### 难题 2：多人同时出价时页面状态错乱

常见问题：

- 当前价更新了，但排行榜没更新
- 排行榜更新了，但“我是否领先”没更新
- 用户会看到前后不一致的状态

解决技巧：

- 前端把状态拆分清楚：房间状态、用户状态、事件消息
- 所有实时消息都尽量带上时间戳或版本号
- 旧消息不覆盖新消息
- 首次进入直播间时先请求一次完整快照，再接收增量消息

#### 难题 3：WebSocket 断连重连

常见问题：

- 网络抖动后用户看不到最新状态
- 连接恢复后页面还是旧数据

解决技巧：

- 使用支持自动重连的 `Socket.IO Client`
- 重连成功后立刻重新拉取一次房间快照
- 增加连接状态提示，例如“重连中”
- 关键页面要有“兜底刷新”逻辑

#### 难题 4：用户疯狂点击出价按钮

常见问题：

- 重复请求过多
- 前端以为自己出价成功，实际后端拒绝了

解决技巧：

- 前端做按钮节流
- 请求发送后短暂禁用按钮
- 必须以后端返回结果为准，不要前端先假设成功
- 对失败情况给出明确提示，例如“当前价格已变化，请重新出价”

### 3.6 前端适合 AI 协助的部分

- 页面骨架生成
- 通用组件编写
- 表单页面生成
- 接口请求封装
- Socket 客户端封装
- 基础动画和提示组件

### 3.7 前端自己必须重点理解的部分

- 出价按钮点击后完整发生了什么
- 为什么倒计时必须以服务端为准
- 为什么要区分“页面显示状态”和“服务端真实状态”
- 为什么重连后要重新同步一次房间快照

---

## 4. 后端提纲

### 4.1 后端目标

后端是整个系统的核心，负责：

- 保存商品和竞拍数据
- 校验出价是否合法
- 维护实时排行榜
- 保证多人并发出价时的数据一致性
- 向前端广播最新竞拍状态
- 在竞拍结束后生成订单

### 4.2 后端推荐技术栈

- `Node.js`
- `NestJS`
- `MySQL`
- `Redis`
- `Socket.IO`
- ORM：`Prisma` 或 `TypeORM`
- 接口文档：`Swagger`
- 参数校验：`class-validator`
- 压测工具：`k6` 或 `autocannon`

为什么推荐 `NestJS`：

- 结构清晰，模块化明显
- 更像“工程化后端”
- 很适合做控制器、服务、网关、模块拆分
- AI 对它的常见写法支持比较好

### 4.3 后端模块拆分

- 用户模块
- 商品模块
- 竞拍模块
- 出价模块
- 排行榜模块
- 订单模块
- WebSocket 网关模块
- 定时任务模块
- 日志与监控模块

### 4.4 后端数据表建议

#### 用户表 `users`

字段示例：

- `id`
- `nickname`
- `avatar`
- `role`
- `created_at`

#### 竞拍商品表 `auction_items`

字段示例：

- `id`
- `title`
- `cover_image`
- `description`
- `start_price`
- `increment`
- `max_price`
- `duration_seconds`
- `extension_seconds`
- `status`

#### 竞拍场次表 `auction_sessions`

字段示例：

- `id`
- `item_id`
- `room_id`
- `start_time`
- `end_time`
- `current_price`
- `leader_user_id`
- `status`

#### 出价记录表 `bids`

字段示例：

- `id`
- `session_id`
- `user_id`
- `bid_price`
- `request_id`
- `created_at`

#### 订单表 `auction_orders`

字段示例：

- `id`
- `session_id`
- `winner_user_id`
- `final_price`
- `status`
- `created_at`

### 4.5 后端核心业务流程

#### 流程 1：创建竞拍

- 商家创建商品
- 配置竞拍规则
- 创建竞拍场次
- 等待开始

#### 流程 2：用户出价

- 用户发起出价请求
- 后端校验竞拍是否进行中
- 校验价格是否符合加价规则
- 校验是否超过封顶价
- 成功后更新当前价和领先人
- 写入出价记录
- 广播最新竞拍状态

#### 流程 3：自动延时

- 如果接近结束时间有人出价
- 服务端自动延长结束时间
- 广播“竞拍延时”消息

#### 流程 4：竞拍结束

- 服务端判断时间到达
- 锁定竞拍状态为结束
- 确认最终领先者
- 生成订单
- 广播成交结果

### 4.6 后端难题

#### 难题 1：高并发下多人同时出价

这是整个项目最核心的难题。

问题本质：

- 100 个人可能同时抢同一个价格
- 如果处理不好，会出现两个用户同时出价成功
- 会导致当前价、领先者、排行榜都错

解决技巧：

- 热点竞拍数据优先放在 `Redis`
- 出价逻辑使用原子操作处理
- 可以使用 Redis 的 `Lua` 脚本，把“校验 + 更新 + 记录结果”放在一次操作里
- 锁的粒度要小，只锁单个竞拍场次，不要锁整个系统

#### 难题 2：数据一致性

常见问题：

- Redis 里的当前价和 MySQL 里的记录不一致
- 当前价更新了，但出价记录没成功写入数据库

解决技巧：

- Redis 承担高频实时读写
- MySQL 负责最终持久化
- 关键流程按“先原子更新实时状态，再异步或同步落库”的方案设计
- 竞拍结束时进行一次最终结算校验
- 保留操作日志，方便排查问题

#### 难题 3：出价幂等性

常见问题：

- 用户点了两次按钮
- 网络超时后前端重试了一次
- 后端把一次出价处理成两次

解决技巧：

- 每次出价请求都带一个唯一 `requestId`
- 后端记录已经处理过的 `requestId`
- 相同 `requestId` 再次到来时直接返回已有结果
- 这是防止“重复扣款”“重复出价”的关键手段

#### 难题 4：自动延时和结束冲突

常见问题：

- 用户在最后一秒出价
- 一个线程想结束竞拍，另一个线程想延长时间
- 最后状态可能冲突

解决技巧：

- 竞拍结束判断必须由服务端统一处理
- 延时逻辑和结束逻辑都要作用于同一个竞拍状态
- 通过状态机和原子更新确保某一时刻只有一个正确结果

#### 难题 5：WebSocket 广播压力

常见问题：

- 房间里人数多时，广播频繁
- 服务端压力增大
- 不同直播间互相影响

解决技巧：

- 按直播间做房间隔离
- 只给同房间用户广播消息
- 区分“全量状态”和“事件消息”
- 不要每次都广播过大的数据
- 可以按事件类型精简消息结构

### 4.7 后端非常关键的设计点

#### 状态机设计

建议把竞拍状态设计成明确的状态机：

- `pending`：未开始
- `running`：进行中
- `ended`：已结束
- `cancelled`：已取消

好处：

- 能防止非法操作
- 已结束竞拍不能继续出价
- 已取消竞拍不能生成订单

#### 房间隔离设计

每个竞拍房间单独管理：

- 单独维护连接
- 单独广播消息
- 单独维护排行榜

这样更适合扩展到多直播间。

#### 服务端时间统一

倒计时和结束判断都必须以服务端时间为准：

- 不能相信客户端本地时间
- 不能让前端决定竞拍结束

#### 最终一致性思路

可以这样理解：

- Redis 负责快
- MySQL 负责准和留痕

这是高并发系统里很常见的设计思路。

### 4.8 后端适合 AI 协助的部分

- NestJS 项目骨架生成
- 控制器和模块模板生成
- DTO 和参数校验代码生成
- 基础 CRUD 生成
- Swagger 注解生成
- WebSocket 网关基础代码生成

### 4.9 后端自己必须重点理解的部分

- 为什么出价逻辑不能简单“先查数据库再更新数据库”
- 为什么高并发下需要 Redis
- 为什么要做幂等
- 为什么要做状态机
- 为什么同一个竞拍场次要尽量原子化处理

---

## 5. 高并发专题理解

这是答辩里很重要的一部分，建议单独准备。

### 5.1 什么叫高并发

在这个题目里，高并发主要指：

- 很多用户在短时间内同时出价
- 很多用户同时接收实时消息
- 系统需要在短时间内频繁更新价格、排名、倒计时状态

### 5.2 高并发下最容易出的问题

- 多人同时出价成功
- 排行榜不一致
- 倒计时不同步
- 接口被重复请求
- 数据库压力过大
- WebSocket 消息过多导致服务卡顿

### 5.3 面对高并发的常见技巧

- Redis 缓存热点数据
- 使用原子操作
- 使用分布式锁或 Lua 脚本
- 做请求幂等
- 房间级消息隔离
- 把重实时、轻历史的数据优先放内存或缓存层处理
- 最终结果落 MySQL 持久化

### 5.4 竞赛答辩时可以怎么讲

你可以用很朴素的话解释：

- 我们把“实时价格、当前领先人、排行榜”放在 Redis，是因为这些数据变化非常频繁
- 我们用原子操作处理出价，是为了防止多人同时出价时产生脏数据
- 我们给每次出价加 `requestId`，是为了避免重复请求导致重复处理
- 我们把 WebSocket 按房间隔离，是为了防止一个直播间的高流量影响其他直播间
- 我们让服务端统一决定倒计时和竞拍结束，是为了保证所有客户端看到的结果一致

---

## 6. 适合小白的开发节奏

### 第一阶段：先跑通最小闭环

目标：

- 用户进入直播间
- 看到商品和当前价
- 点击出价
- 其他用户页面实时看到价格变化

这一阶段先不要做太多花哨功能。

### 第二阶段：补规则

补齐：

- 起拍价
- 加价幅度
- 封顶价
- 自动延时
- 取消竞拍

### 第三阶段：补体验

补齐：

- 实时排行榜
- “领先了”提示
- “被超越了”提示
- 倒计时动画
- 出价音效

### 第四阶段：补工程能力

补齐：

- 断线重连
- 幂等处理
- Redis 优化
- 压力测试
- 操作日志
- 异常兜底

---

## 7. AI 辅助开发建议

这个项目很适合边学边用 AI，但要注意分工。

### 7.1 可以多让 AI 做的事情

- 搭项目脚手架
- 生成页面代码
- 生成基础接口
- 生成数据库表结构建议
- 生成 DTO、表单、管理页
- 生成测试样例
- 帮你解释报错

### 7.2 不能完全交给 AI 的事情

- 出价规则是否合理
- 高并发处理方案是否正确
- 状态机是否完整
- 幂等逻辑是否真的生效
- 关键边界条件有没有遗漏

### 7.3 最好的使用方式

推荐采用下面的节奏：

1. 先自己说清楚“我要做什么”
2. 让 AI 帮你生成第一版代码
3. 自己看懂每个关键函数在干什么
4. 发现问题后让 AI 继续迭代
5. 关键逻辑亲自验证，不要直接相信 AI

---

## 8. 最后建议

对于这个竞赛，你们不一定要把所有功能都做满。

更好的策略是：

- 基础闭环必须完整
- 规则必须正确
- 实时效果必须明显
- 重点突出 1 到 2 个技术亮点

推荐优先打磨的亮点：

- 实时竞拍体验
- 高并发出价一致性
- 自动延时机制
- 断线重连和状态恢复

如果时间不够，宁可把一个亮点做到扎实，也不要每个点都只做一半。

---

## 9. 补充提醒

题目 PDF 中出现了一段看起来像密钥的信息。

建议：

- 不要把这类内容提交到公开仓库
- 不要随意发到群里
- 如果后续项目里要接入第三方 API，优先放到环境变量中管理

