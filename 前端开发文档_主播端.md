# 前端开发文档

## 1. 文档目标

这份文档只针对“主播端前端（商家/主播 PC 管理后台）”。

目标是帮助你把页面结构、交互流程、状态变化和技术方案一次梳理清楚，保证前端原型、后端接口、答辩口径三者一致。

本项目主播端的核心任务是：

- 发布竞拍商品
- 配置竞拍规则
- 管理拍品状态与进度
- 处理成交订单
- 查看直播内各拍品流量与出价趋势
- 支持异常竞拍干预（取消、修改未开拍规则）
- 为用户端与后端联调提供稳定运营入口

---

## 2. 做 H5 还是小程序

### 2.1 结论

这次比赛建议优先做：`PC Web 管理后台`

### 2.2 原因

- 主播端是运营工作台，核心操作依赖表格、表单、看板、图表
- `React + TypeScript` 生态成熟
- AI 辅助生成后台页面和表单逻辑更稳定
- 与后端管理接口联调更方便
- 数据看板、折线图、批量操作在 PC 端体验更好

### 2.3 小程序是否要做

可以作为后续扩展方向，但不建议当前版本优先投入。

---

## 3. 前端技术方案

### 3.1 推荐技术栈

- 框架：`React`
- 语言：`TypeScript`
- 构建工具：`Vite`
- 路由：`React Router`
- 状态管理：`Zustand`
- 服务端状态：`TanStack Query`
- 请求库：`Axios`
- 实时通信：`Socket.IO Client`
- UI 组件：`Ant Design`
- 动画：`Framer Motion`
- 样式：`SCSS` 或 `CSS Modules`

### 3.1.1 路由落地约定

本次主播端第一版建议只保留 `5` 个真实路由：

- `/login`
- `/dashboard`
- `/publish`
- `/goods`
- `/orders`

说明：

- `商品编辑抽屉 / 取消确认弹窗 / 规则说明 / 发布成功提醒 / 异常预警提醒 / 成交结果面板 / 数据趋势弹窗` 统一作为页面内状态或弹层实现，不单独拆成真实路由
- 这样更符合后台操作流，也更方便后续把实时状态集中挂在商品管理页和数据看板页上

### 3.2 状态管理建议

建议分成 4 层状态：

- 主播账号与权限状态
- 商品与当前场次状态
- 订单与成交状态
- UI 状态

---

## 4. 主播端产品结构

这个主播端不是传统电商“内容运营后台”的结构，而是“直播拍卖运营中台”的结构。

核心路径应理解为：

`登录 -> 运营总览 -> 竞拍发布 -> 商品管理（开拍前修改 / 竞拍中监控 / 异常取消）-> 成交后订单查看 -> 数据趋势复盘`

此外还有一条辅助路径：

`商品管理 -> 查看单个拍品实时趋势 -> 返回商品管理继续操作`

侧边导航约定：

- 主播端侧边导航保留 `4` 栏：`总览`、`竞拍发布`、`商品管理`、`订单管理`
- `总览` 对应全局状态和数据趋势入口
- `竞拍发布` 对应新拍品与规则配置
- `商品管理` 对应拍卖过程管理与异常干预
- `订单管理` 对应成交订单与履约信息

---

## 5. 页面总览

建议主播端总共做 `12` 个页面或状态稿。

其中：

- 正常页面：`5` 个
- 弹层 / 面板页面：`7` 个

---

## 6. 页面清单

## 6.1 正常页面（5 个）

### 页面 1：登录页

作用：

- 主播或商家通过账号密码进入后台

主要内容：

- 品牌或系统标题
- 手机号/账号输入框
- 密码输入框
- 登录按钮
- 协议勾选区
- 忘记密码或帮助入口

说明：

- 当前版本使用“账号 + 密码”真实登录样式
- 接口口径按后端 `POST /auth/login` 设计对齐
- 但前端第一版仍建议先接 `mock` 数据把页面流程跑通，再切换真实接口

---

### 页面 2：运营总览页

作用：

- 展示直播拍卖整体运行状态
- 快速进入发布、管理、订单模块

主要内容：

- 顶部筛选区（直播场次、时间范围）
- 核心统计卡片（在拍中数量、已成交数量、流拍数量、成交总额）
- 当前直播拍卖进度看板
- 竞拍数据趋势入口
- 最近异常提醒列表

说明：

- 这页风格参考标准管理后台 dashboard
- 第一版即使统计维度不多，也要保留总览结构
- 点击功能卡片后进入对应模块
- 进入总览后，前端需同时加载：
  - 当前直播场次下全部拍品状态
  - 当前拍品实时进度摘要

这里要统一两个概念：

- `商品台账`：用于主播查看全部拍品基本信息、状态、规则配置
- `当前激活竞拍场次`：当前真正处于竞拍中的那一个对象，实时进度、流量曲线、异常提醒都以它为准

---

### 页面 3：订单管理页

作用：

- 查看成交后自动生成的订单
- 跟进订单进度和履约信息

主要内容：

- 订单筛选区（订单状态、时间、拍品、买家）
- 订单列表表格
- 成交金额
- 买家信息摘要
- 物流状态摘要

说明：

- 订单信息放在订单管理页内，不再拆单独详情路由
- 页面侧边导航固定保留 `总览`、`竞拍发布`、`商品管理`、`订单管理`
- 从订单页返回商品管理时，固定返回“上一次操作的商品列表筛选状态”
- 前端需额外保存 `lastGoodsFilterState`，用于后台操作连续性

---

### 页面 4：竞拍发布页

这是整个主播端最重要的页面之一。

作用：

- 新增拍卖品
- 配置竞拍规则
- 提交到待上架队列

页面结构建议：

- 顶部页面标题与发布说明
- 商品基础信息表单区
- 商品图片上传区
- 竞拍规则配置区
- 发布预览区
- 底部操作区（保存草稿、发布）

注意：

- 竞拍发布页必须突出“基础信息 + 规则配置”两块
- 规则配置应作为独立表单区，而不是混在描述输入区域
- 支持配置：起拍价、加价幅度、竞拍时长、封顶价、延时机制
- 起拍价默认 `0 元`，但允许主播自定义
- 封顶价允许为空，表示“无封顶”
- 延时机制建议默认“结束前 30 秒有人出价延时 15 秒”，并支持后台改参数
- 发布成功后默认进入商品管理页并定位到新商品
- 发布页需保留 `规则说明` 按钮，便于主播核对口径

---

### 页面 5：商品管理页

作用：

- 查看所有竞拍商品状态、进度、成交结果
- 对未开始拍品进行规则修改
- 取消异常竞拍

页面风格参考你给的后台管理图，但要改成竞拍语境。

  主要内容：
  
  - 顶部筛选（状态、直播场次、时间）
  - 商品列表表格或卡片
  - 当前状态（待上架 / 竞拍中 / 已成交 / 已流拍）
  - 实时进度（当前价、剩余时间、出价次数）
  - 规则字段快捷展示（起拍价 / 加价幅度 / 封顶价 / 时长）
  - 操作列（编辑规则 / 取消竞拍 / 查看结果）
  - 竞拍数据趋势入口
  - 异常状态提示区

操作区建议包含：

- 编辑规则按钮（仅未开始可用）
- 一键取消按钮（仅未开始可用，异常场次允许人工取消）
- 查看结果按钮（已成交 / 已流拍）
- 查看趋势按钮（查看该拍品流量与出价轨迹）

说明：

- 点击商品行可查看该拍品完整信息
- 点击 `编辑规则` 时拉起编辑抽屉
- 商品管理页是独立真实路由 `/goods`
- 商品规则修改应根据拍品状态切换权限，而不是始终可编辑
- 对 `竞拍中` 拍品默认只读，避免误操作破坏比赛演示稳定性

---

## 6.2 弹层 / 面板页面（7 个）

### 页面 6：商品编辑抽屉

作用：

- 从商品管理页侧边拉出
- 在不跳转页面情况下查看和编辑单个拍品
- 提供状态相关操作入口

结构建议：

- 半透明背景遮罩
- 右侧抽屉面板
- 顶部标题和状态标签
- 可滚动规则配置区
- 底部操作按钮区

每个商品编辑面板建议包含：

- 商品图
- 商品标题
- 起拍价 / 当前价
- 状态标签
- 规则字段编辑区
- 操作按钮区

交互定义：

- 点击 `编辑规则`：打开商品编辑抽屉
- 点击 `保存`：提交规则修改
- 当商品状态为 `待上架 / 未开始 / 即将开始` 时：允许修改规则
- 当商品状态为 `竞拍中 / 已成交 / 已流拍 / 已取消 / 已结束` 时：仅允许查看

拍品状态建议包括：

- `待上架`
- `未开始`
- `即将开始`
- `竞拍中`
- `已成交`
- `已流拍`
- `已取消`
- `已结束`

补充口径：

- 上一件结束后，下一件默认先进入 `即将开始`
- 不是系统自动开拍
- 必须等主播或商家手动开始后，状态才切成 `竞拍中`
- 只有 `未开始` 和 `即将开始` 的拍品允许规则调整
- 其他状态不显示可提交的规则修改操作

---

### 页面 7：规则配置面板

作用：

- 主播配置竞拍核心参数并提交

主要内容：

- 起拍价输入
- 封顶价输入（可为空）
- 固定加价幅度输入
- 竞拍总时长输入
- 延时机制参数输入
- 规则预览说明
- 提交按钮

当前版本业务规则要在前端明确展示：

- 起拍价支持 `0 元`
- 加价幅度按拍品单独配置
- 每次出价只能是加价幅度的整数倍
- 封顶价为空时展示为“无封顶”
- 最后 `30s` 内有人出价，在原时间基础上 `+15s`（主播端默认配置）
- 达到封顶价时直接成交

建议至少支持 4 个状态：

- 正常可编辑
- 草稿未提交
- 发布前二次确认
- 已发布后锁定（仅未开拍可再次编辑）

补充交互约定：

- 规则配置面板只在 `新建 / 未开始` 状态下可提交
- 如果拍品不是 `未开始`，前端不展示可提交规则按钮
- `智能出价` 属于用户端能力，主播端当前版本不实现自动策略配置，只保留规则描述字段

---

### 页面 8：竞拍规则说明弹窗

作用：

- 向主播清晰说明当前场次规则口径

内容建议：

- `0 元起拍`
- 每次至少加价 `X` 元
- 封顶价为 `Y` 或 `无封顶`
- 最后 `30s` 内出价，自动延长 `15s`
- 达到封顶价直接成交
- 异常情况下主播可取消场次

实现口径补充：

- 规则信息优先作为发布页和管理页中的一个独立按钮入口
- 第一版不强依赖单独的规则弹窗交互
- 若实现成本合适，可在点击 `规则说明` 后使用轻量弹层或抽屉展示

---

### 页面 9：发布成功 / 更新成功提示弹层

作用：

- 主播发布拍品或更新规则成功后给出反馈

主要内容：

- 大标题反馈
- 商品名称
- 生效范围说明
- 返回商品管理或继续发布提示

---

### 页面 10：异常竞拍确认弹层

作用：

- 当主播需要取消异常场次时，进行二次确认，避免误操作

主要内容：

- 取消风险提示
- 当前商品状态
- 当前最新价
- 取消原因输入（可选）
- 确认取消按钮

设备能力建议：

- 取消完成后可调用浏览器通知能力提醒运营人员
- H5/PC 端优先尝试 `Notification`
- 若浏览器不支持则优雅降级

---

### 页面 11：竞拍结束结果面板

作用：

- 当前拍品竞拍结束后，向主播展示结果

至少支持 3 种状态：

- 成交
- 流拍
- 异常取消

---

### 页面 12：竞拍数据趋势面板 / 折线图弹层

作用：

- 展示直播期间多个拍品的流量变化和出价攀升轨迹

建议形式：

- 总览页内嵌折线图卡片
- 或商品管理页右侧弹出趋势面板

还可以配合：

- 多拍品切换图例
- 时间轴缩放

---

## 7. 页面关系图

主流程：

`登录页 -> 运营总览页 -> 竞拍发布页 -> 商品管理页`

从商品管理页可进入：

- 商品编辑抽屉
- 规则配置面板
- 竞拍规则说明弹窗
- 异常竞拍确认弹层
- 竞拍结束结果面板
- 数据趋势面板
- 订单管理页

商品管理页会触发：

- 发布成功弹层
- 更新成功弹层
- 异常竞拍确认弹层
- 竞拍结束结果面板

辅助流程：

`运营总览页 -> 订单管理页 -> 查看成交记录 / 履约信息`

实现补充：

- 真实路由只保留 `登录 / 总览 / 竞拍发布 / 商品管理 / 订单管理`
- 其余都作为页面状态稿或弹层处理
- 侧边导航统一使用 `总览 / 竞拍发布 / 商品管理 / 订单管理`

---

## 8. 关键交互定义

### 8.1 商品管理交互

- 点击商品行：查看商品详情与当前竞拍进度
- 点击 `编辑规则` 按钮：打开商品编辑抽屉
- 只有当状态为 `待上架 / 未开始 / 即将开始` 时，才显示可提交规则按钮
- 当状态为 `已成交 / 已流拍 / 已取消 / 已结束` 时，按钮显示为 `查看结果`
- 当状态为 `竞拍中` 时，按钮显示为 `监控中（只读）`

状态说明：

- 如果拍品处于 `竞拍中`，主播端只能监控，不能修改核心规则
- 只有状态为 `未开始` 或 `即将开始` 的拍品才能进入有效规则修改流程

### 8.2 看板交互

当前版本固定为：

- 全部拍品状态分布
- 当前进行中拍品进度
- 关键成交指标

### 8.3 订单记录交互

- 当前场次结束结果是“本场结果”
- 成交订单记录放在订单管理页内

### 8.4 数据统计与趋势分析

这个功能当前版本不一定要完整实现复杂分析，但前端必须留出入口和状态位。

建议预留：

- 总览页的 `数据趋势` 按钮
- 状态管理中的图表筛选字段
- 后续可扩展的多指标叠加（流量、出价次数、价格轨迹）

说明：

- 这是后续必做功能
- 当前版本优先实现折线图基础展示，不做复杂预测模型

---

## 9. 前端状态设计建议

### 9.1 主播状态

- 主播 id
- 昵称
- 头像
- 是否已登录

### 9.2 直播场次状态

- 直播间 id
- 当前激活场次 id
- 当前拍品 id
- 当前价格
- 当前领先者
- 结束时间
- 在线人数

### 9.3 商品状态

- 商品队列列表
- 每个商品详情
- 当前商品状态
- 当前编辑中的商品 id

### 9.4 订单与成交状态

- 成交订单列表
- 订单状态
- 成交金额
- 买家信息
- 物流状态
- 异常订单标记

### 9.5 UI 状态

- 当前打开哪个弹层
- 是否正在发布
- WebSocket 是否连接
- 是否正在重连
- 当前商品编辑抽屉是否打开
- 侧边导航当前选中项

---

## 10. 实时消息建议

后端至少推送这些消息：

- `room_list_updated`
- `auction_item_changed`
- `auction_detail_updated`
- `auction_price_updated`
- `auction_ranking_updated`
- `auction_bid_success`
- `auction_overtaken`
- `auction_extended`
- `auction_ended`
- `order_created`
- `online_count_updated`

前端处理原则：

- 所有倒计时以后端时间为准
- 所有竞拍结果以后端返回为准
- 商品状态只展示当前有效状态
- 收到旧版本消息不覆盖新状态
- 通知能力失败时不影响主流程

---

## 11. 组件拆分建议

建议至少拆成这些组件：

- `DashboardStatsCard`
- `DashboardTrendChart`
- `PublishForm`
- `RuleConfigPanel`
- `GoodsTable`
- `GoodsStatusTag`
- `GoodsEditDrawer`
- `GoodsActionBar`
- `RuleModal`
- `PublishSuccessModal`
- `ExceptionCancelModal`
- `AuctionResultPanel`
- `OrderTable`
- `OrderStatusTag`
- `TrendLineChart`
- `AdminHeader`

---

## 12. 数据统计展示建议

你问的 3 类统计，其实可以都做，但建议分层做：

### 第一层：后台静态统计页

这是最容易落地的：

- 总拍品数
- 已成交拍品数
- 流拍拍品数
- 总成交金额

### 第二层：成交与运营统计卡片

展示在主播后台概览页：

- 今日成交额
- 今日出价次数
- 当前直播间在线人数

### 第三层：直播内多拍品折线趋势

这是最有展示效果的一层：

- 一场直播内多个拍品的流量变化轨迹
- 每个拍品从开拍到结束的价格攀升轨迹

建议：

- 三者都可以做
- 但第一版优先做后台静态统计页和成交统计卡片
- 多拍品流量折线和出价轨迹作为增强项补上

---

## 13. 测试建议

重点验证：

- 0 元起拍是否正确配置与保存
- 是否只能按固定加价幅度规则生效
- 无封顶时是否正确显示“无封顶”
- 最后 30 秒出价是否在原时间基础上 +15 秒（按主播端默认延时规则）
- 商品状态是否正确显示为待上架/竞拍中/已成交/已流拍
- 点击商品编辑和点击查看结果是否进入不同流程
- 异常取消是否有二次确认并正确更新状态
- 成交后是否自动生成订单并可在订单页查看

---

## 14. 最终建议

这一版主播端最重要的不是页面数量，而是把以下 5 个点画准并实现准：

- 竞拍发布页（商品基础信息 + 规则配置）
- 商品管理全局看板
- 未开始拍品规则修改与异常取消
- 订单管理闭环
- 多拍品流量趋势与价格轨迹折线图

一句话总结：

这次前端的核心不是“像通用 ERP 一样复杂”，而是“像直播拍卖运营台一样清楚、可控、可复盘”。

补充一句：

- 路由要少而稳定，复杂状态尽量收敛在商品管理页和总览页内部处理，这样更适合比赛版快速落地
